using System;
using System.Collections.Generic;
using System.IO;
using System.Linq;
using System.Runtime.Serialization.Formatters.Binary;
using System.Text;
using System.Threading.Tasks;

namespace Serialisation
{
    class Program
    {
        static void Main(string[] args)
        {


           private const string FICHIER_BINAIRE = @"..\..\blalbla";     // le @ veut dire n'iterprete pas les \ comme pour \n ou \t etc       // le chemin indiqué est relatif , en partant de la place de l'exe
        private const string FICHIER_XML = @"..\fsdf";


        int i = 0;

        switch(i){



            case 1: 
            FileStream flux = File.Create(FICHIER_BINAIRE);
        BinaryFormatter seriali = new BinaryFormatter();
        

        //etc


        case 2 :

        // pour soaop il faut aller dans reference et le cocher en ajoutant une reference (faire recherche soap)


            }

        }
    }
}
